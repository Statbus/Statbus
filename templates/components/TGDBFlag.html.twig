{% if tgdb %}
  <span class="badge progress-bar-striped text-bg-warning" style="--bs-bg-opacity: 1;" title="This data is confidential and should not be shared outside of admin channels">TGDB</span>
{% endif %}
